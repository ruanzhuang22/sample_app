<li>
  <%= gravatar_for user, size: Settings.gravatar.size.index %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t(".delete"), user, data: {turbo_method: :delete, turbo_confirm: t(".you_sure")} %>
  <% end %>
</li>
